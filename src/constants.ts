import { Method } from './types';

export const DEFAULT_FOLDER_PATH = 'dist';
export const DEFAULT_FILENAME = 'dist.zip';
export const DEFAULT_FILE_KEY = 'file';
export const DEFAULT_METHOD: Method = 'post';
